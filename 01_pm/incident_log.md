# 障害一覧（インシデント台帳）

**担当ロール**: PM / インシデントマネージャー  
**最終更新**: 2026-04-27（INC-004追記）

---

## 障害一覧

| 障害ID | 発生日 | 検知工程 | 事象 | 影響範囲 | 再現有無 | 緊急度 | 真因カテゴリ | 真因 | 応急対応 | 恒久対応 | ステータス | 関連再発防止策ID |
|--------|--------|----------|------|----------|----------|--------|------------|------|----------|----------|------------|----------------|
| INC-001 | 2026-04-15 | 初回起動テスト | bash start.sh実行後にブラウザでhttp://localhost:3000を開いても接続できない | 初回起動時のみ・フロントエンドアクセス | 再現あり（フロントエンドコンパイル完了前にアクセス） | 高 | 実装不備 | start.shがフロントエンド起動完了を待たずにURLを表示、ブラウザ自動オープンもなし | 30秒待ってから手動でブラウザアクセス | ヘルスチェックループ追加＋ブラウザ自動オープン | 解決済み | PRV-001 |
| INC-002 | 2026-04-19 | ユーザー操作 | デスクトップの.appが古いままで起動しない・「すでに起動していますが」警告が出て不安になる | デスクトップからの起動・アプリ停止操作 | 再現あり | 中 | 手順不備 | 変更後に.appを再ビルドする手順が未整備。ブラウザからアプリを停止する機能がなかった | — | build_app.sh作成・CLAUDE.mdに自動リビルドルール追記・ブラウザ停止ボタン実装（設定画面）・POST /api/shutdown実装 | 解決済み | PRV-002 |
| INC-003 | 2026-04-19 | Finderからの起動 | StrideEdge.app をFinderから起動しても何も起きない（アプリが立ち上がらない） | デスクトップからの全起動 | 再現あり | 高 | 実装不備 | ①ゾンビロックファイル問題：クラッシュ後に/tmp/strideedge_launcher.lockに死んだPIDが残存し、そのPIDがOSに再利用された場合にkill -0が成功してランチャーが黙ってexit(0)する。②Gatekeeper初回ブロック：アドホック署名のみのアプリはFinderダブルクリック時にmacOSがブロックする | 手動でlock削除・右クリック→開くで承認 | build_app.shのacquireLock()でkill -0に加えてps -o comm=によるプロセス名検証を追加。build_app.shにspctl --addによるGatekeeper whitelist登録ステップを追加（要sudo）。 | 解決済み | —

---

| INC-004 | 2026-04-27 | 起動テスト | .app からの起動でフロントエンドが起動しない（`env: node: No such file or directory`）。さらに連続起動テスト中にテストスクリプト自体が強制終了される | フロントエンド全体・.appからの全起動 | 再現あり | 高 | 実装不備 | ①.app起動はshell環境を引き継がないためnvmのnodeがPATHに存在せず、npm（シバン`#!/usr/bin/env node`）がnodeを見つけられない。②shutdown.pyがos.killpg()でプロセスグループ全体をKILLするため、start_bg.shと同じプロセスグループを持つ呼び出し元（テストスクリプト等）も終了される | — | ①start_bg.shにexport PATH="$NODE_BIN:$PATH"を追加。②Python subprocess + preexec_fn=os.setsid()で起動し、新しいセッションに独立。③shutdown.pyのos.killpg()を除去し個別PID kill + ポートベースkillのみに変更。10回連続起動テストで全件PASS確認済 | 解決済み | PRV-004 |

## 採番ルール

- 障害ID: `INC-XXX`（3桁連番）
- 新規障害発生時はこのファイルに即時追記する

---

## 真因カテゴリ一覧（分類基準）

- 要件不備 / 設計不備 / 実装不備 / テスト不足 / レビュー不足
- 手順不備 / 環境差異 / 設定不備 / 依存関係管理不足 / ログ・監視不足 / ナレッジ共有不足
